pipeline {
    agent none
    stages {
        stage("A") {
            agent any
            steps {
                echo 'A'
                echo "${params.INSTALLER}"
            }
        }
        stage("B") {
            agent any
            steps {
                echo 'B'
                echo "${params.INSTALLER}"
            }
        }
    }
}
